spring:
  profiles:
    active: dev # dev:开发环境 || prod:生产环境
  application:
    name: AutoMotiveMes
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

security:
  jwt:
    expiration: 86400 # 公共过期时间(1天)